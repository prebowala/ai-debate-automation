name: Generate AI Debate Video

on:
  workflow_dispatch: # Allows manual trigger from GitHub tab
  schedule:
    - cron: '0 0 * * 0' # Optional: runs weekly

jobs:
  build-video:
    runs-on: ubuntu-latest
    timeout-minutes: 15 # Fails fast if anything hangs

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies (FFmpeg)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Python Packages
        run: |
          python -m pip install --upgrade pip
          pip install google-genai requests

      - name: Run Pipeline Script
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python main.py

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: final-debate-video
          path: final_debate_output.mp4
